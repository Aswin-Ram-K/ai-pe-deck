# SESSION_STATE.md — AI-PE Deck

**Paused:** 2026-04-21 (after a long iterative session)
**Branch:** `main` (clean — no uncommitted tracked files)
**HEAD:** `aa04571` remote: two-state UI — Big Bang launchpad + reshaped controls
**Main status:** pushed to `origin/main` this session; GitHub Pages deploy triggered and in-progress.

---

## How to resume

```bash
cd ~/Desktop/app
npm start        # local dev (optional — Pages is now primary distribution)
```

### Static deployment URLs (after Pages deploy completes)

- **Deck:** `https://aswin-ram-k.github.io/ai-pe-deck/`
- **Remote:** `https://aswin-ram-k.github.io/ai-pe-deck/remote.html`

Both live behind stable URLs with no LAN-IP dependency. The phone remote and deck peer-to-peer via PeerJS public broker using the static ID `ai-pe-deck-aswin-ram-k-ece563` — both sides can load from Pages and still connect.

The local `npm start` server (`http://localhost:3002` or whatever port auto-binds) remains a valid alternate for dev/testing; the `/api/claude` + `/api/save-notes` endpoints only exist on the local server, not on Pages.

### GitHub Actions

Workflow: `.github/workflows/pages.yml` → deploys `public/` on every push to `main`. Latest run: `https://github.com/Aswin-Ram-K/ai-pe-deck/actions` (status was `in_progress` at session end; should complete within 2 minutes).

---

## Ephemeral state on disk

| File | Status | Keep? |
|---|---|---|
| `SPEAKER_SCRIPT.md` (repo root, ~27KB) | Untracked since created externally 2026-04-21 05:47 | **User's call** — separate from `public/speaker-notes.json` (13-paragraph JSON). Contains longer speaker script. See *Open question* §2 below about whether to use this for the teleprompter. |
| `.claude-logs/` | Gitignored (per CMP telemetry isolation rule) | Stays local |
| `_shots/` | Gitignored | Dev-only screenshot archive |
| `exports/` | Gitignored | PPTX + notes PDF outputs |
| `node_modules/` | Gitignored | Regenerated by `npm ci` |

---

## Last user prompts (VERBATIM, reverse-chronological — these drove the handoff)

These are the prompts at the very end of the session, immediately before pause. Use to orient the next session on intent.

> **[final request, dictated, partially garbled speech-to-text]**
> "Okay, now I want both the remote and the deck map to be static ID with the help of either git pages. I also want the big bang button, once I press it, to start a 5-second timer, after which the animation goes off. I want the timer to pop up on the remote side on the device as it goes off. After that, I want the script and the speaker's note with everything that I've created for this project From a different instance of God called the Christmas in this world that is a very well-formed community of God. Speakers are not so close, as a slowly scrolling field on the remote land. Technically speaking, that reveals the slave of who we believe is saying, status bar that is connected to the rest, and give me the scrolling feed of the script before that, and then for the explosion. Still, the scrolling feed doesn't mean that I still can't roll it, but for the feed itself, start with a slow pace that you think is reasonable; then let me see the base on my remote. I decide whether it is good. Before you make these rolling feed and the countdown changes, come to all these changes on the little board and push it to the repository on my GitHub before you proceed with any changes that I have discussed right now. And also, watch it and don't make any changes or discuss. Save all of these as kind of like a pick up or a hand off file in a.md format that my next one session is going to pick up and utilize to proceed with the ideas I just discussed with you."

**Decoded intent** (dictation garbling accounted for):

- Deploy both deck + remote via GitHub Pages for static URLs → **DONE this session**
- On the remote, tapping BIG BANG should trigger a **5-second visible countdown** on the phone BEFORE the deck's cosmic sequence fires → **QUEUED for next session**
- During/after the countdown + explosion, the remote should show a **slowly-scrolling speaker-script feed** (teleprompter) — "from a different instance of God called the Christmas" is speech-to-text garbling for something like "from a different instance of Claude" (likely the Claude session that produced `SPEAKER_SCRIPT.md`) → **QUEUED for next session**
- Manual next/prev controls must still exist — teleprompter is additive, not replacement → **QUEUED**
- Initial scroll pace: Claude picks a reasonable slow default; user tunes after seeing it → **QUEUED**
- Order of operations: **push first, create handoff, STOP. Do not implement countdown or teleprompter this session.** → **EXECUTED**

### Earlier user prompts from this session (summary, not verbatim)

Recent iterations on the cosmic intro (in order):
- v1: Asked for pink/purple realistic star, not 5-point SVG star
- v2: Asked for tension/release cosmic arc — slow wake, pulse, implode, hold, flash, explode
- v3: Asked for screen-wide particle cloud (no sphere visible), radial pulse wave propagating from center
- v4: Fixed ejecta teleport (r=0.5 → r=0) and shrank particles to "minute and light-like"
- v5: "Snappier implosion, smaller + briefer singularity"
- v6: Pointed at "static frame of particles before explosion" — ROOT CAUSE found: `uBurstTime` used `performance.now()/1000` while `uTime` used `THREE.Clock.getElapsedTime()` (mismatched epochs). Fixed.
- v7: "Gap between explosion and color splash" → aurora eruption now overlaps tail of ejecta

Remote UI decisions confirmed:
- Q1=a: circular "BIG BANG" button with pulsing glow
- Q2=i: remote snaps back to State A when returning to Intro
- Q3: top 3/4 for info + controls, bottom 1/4 for huge Next button

Debug episode at end of session:
- Phone remote showed "connected" but BIG BANG button was absent because MCP-controlled Chrome from earlier testing was holding the PeerJS peer ID and stuck on slide 4 ("Taxonomy"). Closing the MCP pages released the peer ID — expected to resolve on phone + deck hard-refresh.

---

## Decisions made this session (with the prompts that drove them)

### D1 — Cosmic intro architecture = Three.js r160 + shader-based plasma, not SVG
**Prompt that provoked:** "I want more of a real, revolving pink and purple star, more astronomically accurate... If the animation capabilities that I'm expecting from you are limited to the tool sets that are available publicly and for free, let me know the freemium options or selectively paid options."
**What landed:** Three.js r160 via importmap + SRI; custom plasma ShaderMaterial; 5-color tulip ring palette; corona shell + particle ejecta system.

### D2 — 5-color palette = existing accent swatches verbatim, no 6th
**Prompt:** "For the tulip color description, I kind of derived the color scheme from a different design system I had already established, so I got it confused. I would like you to do read B, but without adding the sixth color."
**Landed:** Shader uses `['#0B3FB5', '#5B21B6', '#831843', '#064E3B', '#0A0A1F']` verbatim (blue/violet/plum/forest/ink). Ink treated as brightness modulator (not dominant fill) to prevent periodic blackouts.

### D3 — Pacing: 15.5s → 14.4s → 12.6s (three consecutive tightenings based on user feedback)
**Final timeline** (total 12.6s to deck.next(), +1.8s first-slide enter):
```
0.00 - 3.50s  tensioning    (pulse wave 0.8 → 2 Hz)
3.50 - 6.00s  intensifying  (2 → 18 Hz, past perceptual threshold)
6.00 - 6.35s  imploding     (0.35s, particles converge to center)
6.35 - 6.45s  held          (0.10s flash beat only — NOT 1.10s)
6.45 - 6.70s  flashing      (white-out)
6.70 - 7.80s  ejecta-early  (1.1s, explosion in vacuum)
7.80 - 10.50s erupt         (aurora overlaps tail of ejecta)
10.50 - 10.80s settled      (brief hold)
10.80 - 12.60s enter         (deck.next() fires + 1.8s slow enter)
```
Constants: `INTRO_EXPLODE_TO_NEXT_MS = 10800ms` (remote-host.js + deck.jsx patched `_go`).

### D4 — Screen-wide particle cloud, no visible sphere
**Prompt:** "I want the entire screen to be just a nebulous cloud of particles that collapses after I press the big bang button."
**Landed:** Sphere + corona meshes hidden (`visible = false`). 6000 particles distributed across radius 0.25 → 2.2 covering the viewport. Radial pulse wave `sin((t - d/2.5) * f)` propagates from center outward. Particles fade to 0 as they collapse (prevents visible "6000 particles stacked at origin" clump).

### D5 — 25% inter-slide slowdown across the deck + 1.8s slow first-slide enter
**Prompt:** "Let's make the inter-slide transitions just 25% slower, extending every other transition involved in the inter-slides transition proportionately."
**Landed:** Scatterboard CSS durations × 1.25 (e.g. 420→525, 560→700, 460→575, 520→650). JS constants in deck.jsx match (EXIT 575, ENTER 700, DELAY 400, BUFFER 225). First-slide enter uses `body.first-slide-enter` CSS override to force all `[class^="deck-enter-"]` to 1800ms duration, added on Intro → next transition, removed ~2.1s later.

### D6 — Desktop keyboard + Next button + phone all trigger same cosmic sequence
**Prompt:** "For now, I want the desktop control that activates the next slide animation… the next button (the right arrow button)… to initiate the animation and then transition into the next slide."
**Landed:** Single monkey-patch on `deck._go(targetIndex, reason)` — the unified chokepoint for keyboard, on-screen buttons, `deck.next()` API. When `currentSlide.label === 'Intro'` + advancing forward + `.intro-exploding` class not present → dispatch `deck-explode` + schedule real advance at 10800ms. Pending-timeout cancellation guards against double-advance.

### D7 — Universal rule: CMP telemetry isolation
**Prompt:** "Make it a universal rule where none of the Claude portal dashboard details get included in any external IDEs, tools, or framework layers ever used with Claude Agent."
**Landed:** New BLOCKING rule in `~/.claude/CLAUDE.md` §"Claude Master Portal Telemetry Isolation". `.claude-logs/` stays gitignored everywhere; README inside it corrected to "local-only." Memory file also created in `~/.claude/projects/-Users-aswinram/memory/feedback_cmp_telemetry_isolation.md`.

### D8 — Remote two-state UI
- State A (deck on Intro): circular pink/purple BIG BANG button with pulsing glow
- State B (deck anywhere else): compact slide number + italic label at top, Prev button + Home/End/Rescan row in middle, edge-to-edge Next button in bottom 25% of viewport
- Auto-switches via `setSlide(index, total, label)` when label changes between 'Intro' and anything else
- On return to Intro (via Prev from Title), remote snaps back to State A

---

## Open threads — **NEXT SESSION PICKS UP HERE**

### Thread 1 — 5-second countdown on BIG BANG tap (queued)

**Status:** queued, not started.

**Spec:**
- When user taps BIG BANG on remote, instead of immediately sending `{action:'start'}`:
  - Start a visible 5-second countdown on the phone itself
  - Display big numbers counting down: 5 → 4 → 3 → 2 → 1
  - At end of countdown, send `{action:'start'}` to the deck
  - Deck then runs the existing cosmic intro

**Implementation hint:** add a "State C — counting-down" to `remote.js` between State A and State B. State A's BIG BANG button `click` handler transitions to State C. State C has a setInterval ticking every 1000ms, updating a large numeral (similar typography to the slide number but much bigger — 200px+ font). Send `{action:'start'}` at count=0. Phone should stay in State C until deck reports label ≠ 'Intro' (i.e., until first-slide enter fires at t=10.8s), then transition to State B.

**Open question:** Does the countdown take over the full screen (replacing the BIG BANG button), or does it overlay with the button dimmed? Recommended: full-screen takeover — no ambiguity about what's happening.

### Thread 2 — Scrolling speaker-notes teleprompter (queued)

**Status:** queued, not started.

**User intent (decoded):** After the countdown (or after the explosion — user wording ambiguous, resolve next session), the remote should display a slowly-scrolling feed of the speaker notes for the presentation. Serves as a teleprompter on stage.

**Data sources (ambiguous — resolve before implementing):**
- `public/speaker-notes.json` — 13 strings, one per slide s1-s13. Relatively short (paragraph each). This is what the DECK uses for PPTX export.
- `SPEAKER_SCRIPT.md` in repo root — ~27 KB longer speaker script created in a different Claude session. The user referenced "a different instance of [Claude]" in the dictation. This is probably the intended source.
- **First action next session:** READ `SPEAKER_SCRIPT.md` to understand its structure (per-slide sections? one long flowing script?) before deciding which source to use.

**Pace default:** user said "start with a slow pace that you think is reasonable; then let me see the pace on my remote. I decide whether it is good." Suggested default: ~140–160 words per minute (broadcast/teleprompter norm; slower than conversational 180 wpm). Tunable via a single constant. Start at 140 and get user feedback.

**Layout (to propose to user at start of next session):**
- Option A: Teleprompter sits in the top 3/4 of State B, replacing the slide number + label area. Huge Next button remains at bottom.
- Option B: Teleprompter is a scrolling overlay that covers the full screen in a new State D, swipe up/down to hide/show. Traditional controls always accessible via swipe.
- Option C: Teleprompter sits as a dedicated State C mode entered via a toggle button; default view remains classic State B.

**Concurrency with manual controls:** user explicitly said "the scrolling feed doesn't mean that I still can't roll it" → manual next/prev MUST still work even while teleprompter scrolls. Teleprompter is read-only; manual advance overrides scroll position (jumps to next slide's notes).

**Relationship to the cosmic intro:** user said "reveals the status bar that is connected to the rest, and give me the scrolling feed of the script before that, and then for the explosion." Translation uncertain but likely means: during the 5-second countdown AND/OR during the 12.6s cosmic intro, the feed should be visible. Next session should propose two pacing options (teleprompter starts at countdown t=0 vs teleprompter starts when deck lands on slide 1) and let user pick.

### Thread 3 — Pages deploy verification (in progress at session pause)

**Status:** deploy triggered, was `in_progress` at pause. Run URL: `https://github.com/Aswin-Ram-K/ai-pe-deck/actions`

**At start of next session:** verify the workflow completed green, the deck loads at `https://aswin-ram-k.github.io/ai-pe-deck/`, and the phone remote at the corresponding `/remote.html` URL connects correctly.

---

## Working-tree snapshot

- Branch: `main` clean (no modified tracked files)
- Commits ahead of origin at session start: 12
- Commits pushed this session: 12 (`917d10a..aa04571`)
- Remote HEAD after push: `aa04571`
- Untracked: `SPEAKER_SCRIPT.md` (27KB, 2026-04-21 05:47)

### Tests-at-pause

End-to-end test (phone remote + deck in default browser):
- **State A ✓** — BIG BANG button renders, pulses, connects
- **State B ✓** — compact number + big Next button layout verified via Chrome DevTools MCP at 390×844
- **Cosmic intro end-to-end ✓** — pulse wave → convergence → flash → explosion → aurora eruption → slide 1 slow enter. Verified by ArrowRight trigger on Mac deck.
- **Clock mismatch bug ✓ fixed** (was the real "static frame" root cause)

**NOT verified at pause:**
- End-to-end with real iPhone on Pages URL (next session can verify after Pages deploy completes)
- 5-second countdown (not implemented)
- Teleprompter (not implemented)

---

## Do not forget

- Per global `CLAUDE.md` rule: at session start, the next Claude session MUST display §"Decisions" + §"Last user prompts (VERBATIM)" sections verbatim as the opening of its first response.
- Do NOT implement Thread 1 or Thread 2 features until the user confirms the layout + pacing options at session start.
- `SPEAKER_SCRIPT.md` untracked — next session should decide whether to commit it (it was referenced as the source for the teleprompter).
- The local `npm start` server is optional now that Pages is live. Keep it running only for dev iteration.
- Pages deploys take ~1-2 min per push. If users see an old version, it's likely the Actions workflow is still running or the browser cached the old version (hard-refresh).
